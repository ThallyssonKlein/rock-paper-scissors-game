logging:
  level:
    root: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/testetecnicowefin?allowPublicKeyRetrieval=true
    username: user
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: person-service
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        temp:
          use_jdbc_metadata_defaults: false
  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=1,expireAfterWrite=600s
  sql:
    init:
      mode: never

datadog:
  agent:
    host: localhost
    port: 8125

server:
  port: 8080